// Seleccionando todos los elementos card

const allCards = document.querySelectorAll('.card')
console.log( allCards );


// Selecciono todas las imagenes del html
const AllImages = document.querySelectorAll('img')
console.log( AllImages );

// Selecciono todos los formularios
const forms = document.querySelectorAll('.formulario');
console.log( forms );

// Seleccionando un elemento que no existe en el DOM
const noExiste = document.querySelectorAll('.NoExiste');
console.log( noExiste );