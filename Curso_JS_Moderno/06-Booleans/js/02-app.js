const bool1 = true;
const bool2 = false;

console.log( bool1 === bool2 ); // false

console.log( bool1 === true ); // true
console.log( bool1 === "true" ); // False porque "true" es un string y no un booleano